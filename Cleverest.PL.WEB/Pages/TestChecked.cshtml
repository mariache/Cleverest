@using Cleverest.Common;
@using Cleverest.Models;
@{
    var testLogic = Resolver.TestLogic;
    testLogic.RemoveFromCheck(Container.CurrentTest.Id);
    Container.Clear();
    Response.Redirect("/");

}


